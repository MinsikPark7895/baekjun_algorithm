#include <iostream>
#include <vector>

using namespace std;

vector<int> board;

int main(void) {

    int N;
    int K;

    board.resize(N);

    for (int i = 0; i < N; i++) {
        cin >> board[i];
    }

    

    return 0;
}