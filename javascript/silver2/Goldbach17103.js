const fs = require('fs')
const input = fs.readFileSync(0, 'utf8').trim()
const lines = input.split('\n')

const T = lines[0].split("\n").map(Number)

let arr = []

for (let i = 0; i < T; i++) {
    const N = lines[i + 1].split("\n").map(Number)

    
}
